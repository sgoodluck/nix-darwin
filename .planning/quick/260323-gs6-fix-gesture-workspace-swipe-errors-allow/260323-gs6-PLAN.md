---
phase: 260323-gs6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dotfiles/hypr/hyprland.conf
  - flake.nix
autonomous: true
requirements: [GS6-01, GS6-02, GS6-03]

must_haves:
  truths:
    - "Hyprland starts without gesture-related errors"
    - "Unfree packages can be installed via flake"
    - "Display renders at native 1.0 scale"
  artifacts:
    - path: "dotfiles/hypr/hyprland.conf"
      provides: "Hyprland configuration with correct gestures and scale"
      contains: "monitor = eDP-1, preferred, auto, 1.0"
    - path: "flake.nix"
      provides: "Flake with allowUnfree propagated"
      contains: "allowUnfree = true"
  key_links:
    - from: "flake.nix"
      to: "nixpkgs-nixos"
      via: "overlay/config"
      pattern: "nixpkgs\\.config\\.allowUnfree"
---

<objective>
Fix three NixOS/Hyprland configuration issues: resolve any gestures configuration problems, ensure unfree packages work in flake evaluation, and update display scaling to native resolution.

Purpose: Get Hyprland running cleanly without errors and with proper display scaling
Output: Updated hyprland.conf and flake.nix
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@dotfiles/hypr/hyprland.conf
@flake.nix
@nixos/system.nix
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Hyprland config - gestures and display scale</name>
  <files>dotfiles/hypr/hyprland.conf</files>
  <action>
    1. Change monitor scale from 1.8 to 1.0 on line 8:
       `monitor = eDP-1, preferred, auto, 1.0`

    2. Update lid switch handler on line 13 to use 1.0 scale:
       `bindl = , switch:off:Lid Switch, exec, hyprctl keyword monitor "eDP-1, preferred, auto, 1.0"`

    3. The gestures block (lines 118-121) appears syntactically correct for modern Hyprland. Keep it as-is:
       ```
       gestures {
           workspace_swipe = true
           workspace_swipe_fingers = 3
       }
       ```
       If errors persist after rebuild, the issue may be elsewhere (Hyprland version mismatch).

    4. Update GDK_SCALE on line 44 from 2 to 1 to match the new monitor scale:
       `env = GDK_SCALE,1`
  </action>
  <verify>grep -E "(monitor.*eDP-1|GDK_SCALE)" dotfiles/hypr/hyprland.conf | head -5</verify>
  <done>Monitor scale is 1.0, lid switch uses 1.0, GDK_SCALE is 1</done>
</task>

<task type="auto">
  <name>Task 2: Ensure allowUnfree propagates in flake</name>
  <files>flake.nix</files>
  <action>
    The nixos/system.nix already has `nixpkgs.config.allowUnfree = true`, which works for NixOS module evaluation.

    However, for complete coverage (especially for `nix build` and `nix shell` commands outside the system rebuild), add the nixpkgs config overlay at the flake level.

    In the `mkNixosConfig` function, after `system = "x86_64-linux";`, add a nixpkgs config module:

    ```nix
    modules = [
      # Add this as the first module (before hardware-configuration.nix):
      {
        nixpkgs.config.allowUnfree = true;
      }
      # ... rest of modules
    ```

    This ensures allowUnfree is set early in module evaluation, before any package references.

    Note: The existing setting in nixos/system.nix is redundant but harmless. Can be kept for clarity or removed.
  </action>
  <verify>grep -A5 "mkNixosConfig" flake.nix | grep -i unfree || grep "allowUnfree" flake.nix</verify>
  <done>allowUnfree = true appears in flake.nix NixOS configuration</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "1.0" dotfiles/hypr/hyprland.conf` shows monitor scale updated
2. `grep "allowUnfree" flake.nix` shows unfree setting in flake
3. `nixos-rebuild switch --flake ~/nix#x1nano` completes without gesture errors
</verification>

<success_criteria>
- Monitor scale changed from 1.8 to 1.0 in hyprland.conf
- Lid switch handler uses 1.0 scale
- GDK_SCALE updated to match
- allowUnfree explicitly set in flake.nix for NixOS config
- System rebuilds successfully
</success_criteria>

<output>
After completion, create `.planning/quick/260323-gs6-fix-gesture-workspace-swipe-errors-allow/260323-gs6-SUMMARY.md`
</output>
